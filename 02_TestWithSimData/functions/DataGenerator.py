import os.path
import numpy as np
import pandas as pd

if __name__ == "__main__":
    pass

def data_gen(type = 1, length = 10, seed = 42):
    """
    generates a test Dataset with noise.

    :param type: 1 = random spectra (default: 1)
    :type type: int
    :param length: number of spectrapoints to generate (default: 10).
    :type length: int
    :param seed: random seed for reproducibility (default: 42).
    type seed: int
    :return: data frame with spectral intensities
    :rtype: pd data frame
    """

    return pd.DataFrame()
